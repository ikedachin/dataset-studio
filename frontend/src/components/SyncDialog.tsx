import { X } from 'lucide-react'
import { useEffect, useState } from 'react'
import { api } from '../api'
import type { DatasetRecord, DiffChange, SyncRule } from '../types'

export function SyncDialog({record,rules,onClose,onApplied}:{record:DatasetRecord;rules:SyncRule[];onClose:()=>void;onApplied:(record:DatasetRecord)=>void}){const [changes,setChanges]=useState<DiffChange[]>([]);const [error,setError]=useState('');useEffect(()=>{api.sync(record.id,rules,false).then(r=>setChanges(r.changes)).catch(e=>setError(e instanceof Error?e.message:'Sync preview failed'))},[record.id,rules]);return <div className="modal-backdrop"><div className="modal"><button className="modal-close icon ghost" onClick={onClose}><X/></button><p className="eyebrow">MANUAL SYNC PREVIEW</p><h2>Before → After</h2>{rules.length===0?<p className="empty-small">No sync rules are configured. Add rules in Project Settings.</p>:<div className="sync-preview">{changes.map((c,i)=><div className="diff" key={i}><code>{c.path}</code>{c.before!==undefined&&<pre>- {JSON.stringify(c.before)}</pre>}{c.after!==undefined&&<pre>+ {JSON.stringify(c.after)}</pre>}</div>)}</div>}{error&&<p className="error-text">{error}</p>}<div className="modal-actions"><span/><button onClick={onClose}>Cancel</button><button className="primary" disabled={!changes.length} onClick={async()=>{const result=await api.sync(record.id,rules,true);if(result.record)onApplied(result.record);onClose()}}>Apply Sync</button></div></div></div>}
